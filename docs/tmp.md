cerf load会有link过程 gcos加载sef一样需要解析符号链接  因为一个sef可能依赖并调用其它sef的函数，gcos怎么实现符号链接解析的呢？比如其它模块 函数 或者变量的引用！请参考 cos3_qwen.md以及iwasm 和cref 来实现gcos的符号解析！！这些符号解析后(对其它模块符号引用直接转换为逻辑地址)解析出的地址是32位还是16位呢？cref为压缩空间采用了16位，这样定位逻辑相对复杂，因为16位只能寻址64K 所以它最高位标记为全局引用，这样再通过全局引用表找到32位地址进行访问！此外iwasm 预置了系统模块，请参考iwasm也为gcos实现预置模块，相当于给其它模块提供系统API！！！

请深入分析 cref  iwasm 符号解析，并检查 gcos的符号解析功能是否完备！

cref中通过token来定位方法和成员的  iwasm呢？


gcos是在资源受限的卡片上运行，全局引用表预留多少空间呢，怎么动态扩展全局引用表呢？  请基于gcos是在资源受限的卡片上运行 优化代码


请记忆 gcos 运行在资源受限的智能卡上  加载解析的数据 都需要持久化存储在flash中的 包括全局引用表！

请记忆 gcos 运行在资源受限的智能卡上 ！gcos 运行在资源受限的智能卡上 作为设计实现的准则，实现时尽量考虑空间占用情况 gcos加载解析的数据 都需要持久化存储在flash中的 包括全局引用表！

请记忆 gcos 运行在资源受限的智能卡上 ！并重新审视GCOS目前已经完成的实现，分析 是否满足智能卡 低资源 以及数据需要持久化存放flash以防掉电等！比如load加载解析的sef应该是存放在flash  符号解析也是指向flash  全局引用表等也需要持久化存储在flash  这样就需要一个好的flash布局规划，请分析调整实现

全局引用表 固定预留一定空间！然后动态扩展

cref中全局引用表怎么扩展到，全局引用表中的entry 怎么判定有没有使用呢？比如安装应用会解析符号添加引用，但应用删除后怎么回收这些引用entry呢？gcos有没有考虑全局引用entry的回收呢？是怎么实现的？

grt entry 32bit,高8bit存放packageID 或者模块id，也就是 该ref是加载某个mod时创建，所以mod删除时应该回收？

完善应用/模块流程，将GRT entry 回收集成到模块删除流程 ，此外Gcos是否有考虑 一个模块可以实例化出多个应用！？一个模块只解析一次 解析一次后 代码区是多个应用共享的 这个类似cref  请参考cref进行完善

​cos3-qw.md​ 分析cos3-qw.md 分析规范怎么定义 其它模块函数或者其它模块变量访问的？模块ID+成员ID？

cerf load会有link过程 gcos加载sef一样需要解析符号链接  因为一个sef可能依赖并调用其它sef的函数，gcos怎么实现符号链接解析的呢？比如其它模块 函数 或者变量的引用！请参考 cos3_qwen.md以及iwasm 和cref 来实现gcos的符号解析！！这些符号解析后(对其它模块符号引用直接转换为逻辑地址)解析出的地址是32位还是16位呢？cref为压缩空间采用了16位，这样定位逻辑相对复杂，因为16位只能寻址64K 所以它最高位标记为全局引用，这样再通过全局引用表找到32位地址进行访问！此外iwasm 预置了系统模块，请参考iwasm也为gcos实现预置模块，相当于给其它模块提供系统API！！！

